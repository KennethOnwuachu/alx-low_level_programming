#include <stdio.h>

/**
 * Main - Entry point
 *
 * Description: Prints A-Z
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; letter++)
		putchar(ch);
	putchar('\n');
	return (0);
}
